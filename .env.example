# Microsoft Entra app registration (server-side only; never commit real values)
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# Optional: use a tenant ID to limit sign-in to HKU; defaults to "organizations"
MICROSOFT_TENANT_ID=organizations

# Generate at least 32 random bytes, for example: openssl rand -base64 32
OUTLOOK_SESSION_SECRET=
